import { Spinner } from '@heroui/react';
import React from 'react';

const Loading = () => {
    return (
        <div>
            <Spinner></Spinner>
        </div>
    );
};

export default Loading;